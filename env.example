# Convex Configuration for FitBuddy
# Copy this file to .env and fill in your Convex URL

# Get your Convex URL by running:
# npx convex dev (for development)
# or
# npx convex deploy (for production)
#
# The URL will look like: https://your-deployment-name.convex.cloud

# IMPORTANT: In Expo, environment variables must be prefixed with EXPO_PUBLIC_
# to be accessible in your React Native code

# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=dev:adjective-animal-123 # team: team-name, project: project-name

# Convex API URL (ends in .cloud)
EXPO_PUBLIC_CONVEX_URL=https://adjective-animal-123.convex.cloud

# Convex Site URL (ends in .site) - Required for Better Auth
EXPO_PUBLIC_CONVEX_SITE_URL=https://adjective-animal-123.convex.site

# Better Auth Secret - Generate with: openssl rand -base64 32
# Or set via: npx convex env set BETTER_AUTH_SECRET=$(openssl rand -base64 32)
# This will be automatically added to .env.local by Convex

# Instructions:
# 1. Copy this file: cp env.example .env
# 2. Initialize Convex: npx convex dev
# 3. Copy the CONVEX_URL and CONVEX_SITE_URL from the output
# 4. Paste them as EXPO_PUBLIC_CONVEX_URL and EXPO_PUBLIC_CONVEX_SITE_URL in your .env file
# 5. Set BETTER_AUTH_SECRET: npx convex env set BETTER_AUTH_SECRET=$(openssl rand -base64 32)
# 6. Restart your Expo development server





